services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - travel_db:/app
      - ./app:/app/app
    environment:
      - PYTHONUNBUFFERED=1
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

volumes:
  travel_db:
